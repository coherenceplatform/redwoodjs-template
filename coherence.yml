backend:
  type: backend
  index_file_name: index.html
  #url_path: /api
  url_path: /
  repo_path: backend
  #migration: ["rails", "db:migrate"]

  local_packages: ["/app/node_modules"]

  dev: ["PORT=8911 yarn", "rw", "dev"]
  #  prod: ["yarn", "rw-server", "api", "--port", "8911"]
  prod: ["PORT=8911 DATABASE_URL=file:./dev.db LOG_LEVEL=debug yarn", "rw-server", "api", "--port", "8911"]
  
  system:
    dev:
      port: 8911

      #  resources:
      #  - name: db1
      #    engine: postgres
      #    version: 13
      #    type: database
